<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <resultMap id="UserResultMap" type="com.example.demo.model.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="email" column="email" />
        <result property="profileInfo" column="profile_info" />
    </resultMap>

    <select id="selectUserById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="selectUserByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="selectAllUsers" resultMap="UserResultMap">
        SELECT * FROM users
    </select>

    <insert id="insertUser" parameterType="com.example.demo.model.User">
        INSERT INTO users (username, password, email, profile_info)
        VALUES (#{username}, #{password}, #{email}, #{profileInfo})
    </insert>

    <update id="updateUser" parameterType="com.example.demo.model.User">
        UPDATE users SET username=#{username}, password=#{password}, email=#{email}, profile_info=#{profileInfo} WHERE id=#{id}
    </update>

    <delete id="deleteUser" parameterType="long">
        DELETE FROM users WHERE id=#{id}
    </delete>
</mapper>
